import { BillDetailResponseDto } from './bill-detail-response-dto.model';

describe('BillDetailResponseDto', () => {
  it('should create an instance', () => {
    expect(new BillDetailResponseDto()).toBeTruthy();
  });
});
