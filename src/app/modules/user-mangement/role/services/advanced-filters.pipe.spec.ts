import { AdvancedFiltersPipe } from './advanced-filters.pipe';

describe('AdvancedFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvancedFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
